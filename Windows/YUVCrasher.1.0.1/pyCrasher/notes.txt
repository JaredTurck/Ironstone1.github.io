create a file called join.txt, then write: 
	"file '" + args[0] + "'\nfile sample.bin"

ffmpeg.exe
parameters: " -y -f concat -safe 0 -i join.txt -codec copy output.mp4"

delete the join.txt file
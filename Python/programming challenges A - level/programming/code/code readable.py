import socket

[[print("Your Car was travelling at %s MPH!" % round((3600 / (lambda n : n[1] - n[0])([sum([int(time[i])*[3600,60,1][i] for i in range(len(time))]) for time in [print("A car is traveling past a speed camera! Enter time in format HH:MM:SS"), input("First Time: ").split(":"), input("Second Time: ").split(":")][1:]])),2))] if user == "1" else [print(i) for i in [[(("%" + "0" + str(len(code)) + "d") % (n)) for n in range( int("9"*len(code)) ) if False not in [(("%" + "0" + str(len(code)) + "d") % (n)).count(i) == code.count(i) for i in code]] for code in [input("Enter code: ")]][0] ] if user == "2" else [print("Port %s is open!" % (port)) for port in "".join([[socket.setdefaulttimeout(0.02),[(str(port)+",") if socket.socket(socket.AF_INET, socket.SOCK_STREAM).connect_ex((Host, port)) == 0 else "" for port in range(int(input("Start: ")), int(input("End: "))+1)]] for Host in [input("Host: ")]][0][1]).split(",")[:-1]] if user == "3" else "Invalid" for user in [input("Menu:\n1) Speed tracker\n2) Thief\n3) port scanner\n>>> ")]]

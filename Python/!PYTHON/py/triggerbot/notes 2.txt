switch on then off strait away, no toggle

off, on

keyhold count make the var, a second
timeout between checking ticks, so computer isn't so slow

automatically get screen resolution

issue is with counter

user is still pressing key, after cheat is toggled off

instead of looking for a single pixel that matchs, could look for pattern recognition